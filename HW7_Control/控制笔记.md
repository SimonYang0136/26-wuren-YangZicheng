控制笔记：
纯跟踪的公式变形，要留下哪些变量？
最开始拿到运动学模型的时候有点不知道怎么变形，保留哪些参数，因为不清楚每个环节的输入输出是谁；
花了些时间，做了推导，画了控制框图才大概看清楚。推导笔记见图片。
参考输入（目标） → 误差计算 → 控制器（算法） → 被控对象（车辆） → 输出（实际状态）
                    ↑                                     ↓
                    └─────────────────── 反馈（实际状态） ───┘
拉普拉斯变换和传递函数没看懂，暂且用微分方程的解法来解释：
系统能否稳态即x能否趋近于定值，用微分方程解的公式，t趋近于正无穷时x是否为定值可以判断
对于系统的建模，列变化量，控制量（控制器的输出）作为系统的输入。